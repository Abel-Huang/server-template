<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.abelib.tmall.dao.UserDao">
    <resultMap id="userMap" type="User">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="userPassword"  column="user_password"/>
        <result property="note" column="note"/>
    </resultMap>
    <cache/>

    <select id="countUser" resultType="int">
        select count(*) from user_
    </select>
    <!--遍历整个 user_ 表-->
    <select id="listAllUser" resultMap="userMap">
        select * from user_
    </select>

    <!--通过id获取 User-->
    <select id="getUserById" parameterType="int" resultMap="userMap">
        select * from user_
        where id = #{id}
    </select>

    <!--插入，并且主键回填-->
    <insert id="insertUser"  parameterType="User"
            useGeneratedKeys="true"  keyProperty="id">
        insert into user_(user_name, user_password, note) values(#{userName}, #{userPassword}, #{note})
    </insert>

    <!--删除 User-->
    <delete id="deleteUser" parameterType="int">
        delete from user_ where id = #{id}
    </delete>

    <!--更新 User-->
    <update id="updateUser" parameterType="User">
        update user_
        <set>
            <if test="note != null and note != ''">
                note = #{note},
            </if>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="userPassword != null and userPassword != ''">
                user_password = #{userPassword}
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>